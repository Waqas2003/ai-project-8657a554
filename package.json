{
  "name": "unicef-website",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel index.html"
  },
  "dependencies": {
    "parcel": "^2.0.1",
    "tailwindcss": "^3.0.2"
  }
}
```

This project uses Tailwind CSS for styling and Parcel as the bundler. You can run the project by executing `npm start` in the terminal, and it will be available at `http://localhost:3000`.